#include <stdio.h>
/**
 * div - divides two integers
 * @a: first integer
 * @b: second integer
 * return: results
 */
int div(int a, int b)
{
        int results;
        results = a / b;
        return (results);
}
